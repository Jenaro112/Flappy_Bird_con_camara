# ── Dependencias del proyecto ──────────────────────────────
# Versiones congeladas del entorno actual (Python 3.9.6)
# Para regenerar: pip freeze | grep -iE "pygame|opencv|mediapipe|numpy|pillow"
# ───────────────────────────────────────────────────────────

pygame==2.6.1
opencv-python==4.13.0
mediapipe==0.10.35
numpy==2.0.2
Pillow==11.3.0
